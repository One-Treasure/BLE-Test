/* 全局样式 */
page {
    background-color: #f8f8f8;
    padding-bottom: 28rpx;
}

/* 个人信息模块样式 */
.data {
    width: 694rpx;
    height: 188rpx;
    background: #FFFFFF;
    opacity: 1;
    border-radius: 30rpx;
    margin: 28rpx;
    display: flex;
}

/* 个人信息左侧区域样式 */
.data-left {
    width: 134rpx;
    height: 100%;
}

/* 用户头像样式 */
.data-left-avatar {
    position: absolute;
    width: 118rpx;
    height: 118rpx;
    background: #FFFFFF;
    border: 2rpx solid #fff;
    border-radius: 50%;
    opacity: 1;
    margin: 36rpx 0 34rpx 16rpx;
    overflow: hidden;
    display: block;
}

/* 用户头像样式 */
.data-left-avatar image {
    width: 118rpx;
    height: 118rpx;
}

/* 未绑定状态头像头部 */
.data-left-avatar-head {
    width: 46rpx;
    height: 46rpx;
    background: #BDC4CE;
    border-radius: 50%;
    opacity: 1;
    margin-top: 16rpx;
    margin-left: 36rpx;
}

/* 未绑定状态头像身体 */
.data-left-avatar-body {
    width: 86rpx;
    height: 52rpx;
    background: #BDC4CE;
    border-radius: 50%;
    opacity: 1;
    margin-top: 6rpx;
    margin-left: 16rpx;
}

/* 个人信息中间区域样式 */
.data-center {
    width: 494rpx;
    height: 100%;
}

/* 隐藏按钮的样式 */
.btn {
    height: 100%;
    background-color: #fff;
    border: 0;
    padding: 0;
    margin: 0;
    display: flex;
}

.btn::after {
    border: 0;
    padding: 0;
}

/* 未绑定显示文字样式 */
.data-center-unbound {
    width: 112rpx;
    height: 40rpx;
    font-size: 28rpx;
    font-family: PingFang SC;
    font-weight: 500;
    line-height: 40rpx;
    color: #333333;
    opacity: 1;
    margin-top: 76rpx;
    margin-left: 16rpx;
}

/* 用户昵称样式 */
.data-center-username {
    height: 40rpx;
    font-size: 28rpx;
    font-family: PingFang SC;
    font-weight: 500;
    line-height: 40rpx;
    color: #333333;
    opacity: 1;
    margin-top: 52rpx;
    margin-left: 16rpx;
}

/* 用户设备序列号样式 */
.data-center-code {
    height: 34rpx;
    font-size: 24rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 34rpx;
    color: #777777;
    opacity: 1;
    margin-top: 16rpx;
    margin-left: 16rpx;
}

/* 个人信息右侧区域样式 */
.data-right {
    width: 92rpx;
    height: 100%;
}

/* 设置icon区域样式 */
.data-right-set {
    width: 50rpx;
    height: 50rpx;
    margin: 70rpx 16rpx 68rpx 26rpx;
}

/* 设置icon区域图片样式 */
.data-right-set image {
    width: 50rpx;
    height: 50rpx;
}

/* 天气情况区域样式 */
.weather {
    width: 694rpx;
    height: 270rpx;
    background: #FFFFFF;
    box-shadow: 0rpx 6rpx 12rpx rgba(255, 148, 148, 0.06);
    opacity: 1;
    border-radius: 30rpx;
    margin: 0 28rpx 0 28rpx;
    display: flex;
}

/* 滚动视图区域样式 */
/* .weather-scroll{
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
}
 */
/* 天气区域单个模块样式 */
.weather-scroll-item {
    width: 140rpx;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* 天气区域单个图形区域样式 */
.weather-scroll-item-view {
    width: 140rpx;
    height: 140rpx;
    border-radius: 50%;
    opacity: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* 图形区域icon样式 */
.weather-scroll-item-view-icon {
    width: 40rpx;
    height: 40rpx;
}

/* 图形区域icon图片样式 */
.icon {
    width: 40rpx;
    height: 40rpx;
    margin-top: 10rpx;
}

/* 用户未授权的icon样式 */
.weather-scroll-item-view-noaddress-icon {
    width: 42rpx;
    height: 6rpx;
    background: #FFFFFF;
    opacity: 1;
    margin-bottom: 10rpx;
}

/* 图形区域名称样式 */
.weather-scroll-item-view-name {
    height: 28rpx;
    font-size: 20rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 34rpx;
    color: #FFFFFF;
    opacity: 1;
    margin-top: 6rpx;
}

/* 图形区域数据样式 */
.weather-scroll-item-view-data {
    height: 36rpx;
    font-size: 26rpx;
    font-family: PingFang SC;
    font-weight: bold;
    line-height: 32rpx;
    color: #FFFFFF;
    opacity: 1;
    margin-top: 10rpx;
}

/* 用户未授权的数据样式 */
.weather-scroll-item-view-noaddress-data {
    width: 42rpx;
    height: 6rpx;
    background: #FFFFFF;
    opacity: 1;
    margin-top: 20rpx;
}

/* 标题区域样式 */
.weather-scroll-item-title {
    height: 34rpx;
    font-size: 24rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 34rpx;
    color: #333333;
    opacity: 1;
    margin-top: 16rpx;
    text-align: center;
}

/* 状态区域样式 */
.weather-scroll-item-state {
    font-size: 20rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 28rpx;
    color: #777777;
    opacity: 1;
    text-align: center;
}

/* 温度模块样式 */
.temp {
    margin-left: 24rpx;
}

/* 温度图形模块样式 */
.temp-view {
    background: linear-gradient(180deg, #FFDC9F 0%, #F9BE56 100%);
    box-shadow: 0px 6rpx 12rpx rgba(66, 112, 196, 0.26);
}

/* 紫外线模块样式 */
.radiation {
    margin-left: 28rpx;
}

/* 紫外线图形区域样式 */
.radiation-view {
    background: linear-gradient(180deg, #FFAEF7 0%, #FF9FF0 100%);
    box-shadow: 0px 6rpx 12rpx rgba(204, 94, 190, 0.29);
}

/* 湿度模块样式 */
.humidity {
    margin-left: 30rpx;
}

/* 湿度图形区域样式 */
.humidity-view {
    background: linear-gradient(180deg, #B1E9FF 0%, #78E4FF 100%);
    box-shadow: 0px 6rpx 12rpx rgba(148, 216, 239, 0.24);
}

/* PM2.5模块样式 */
.pm {
    margin-left: 28rpx;
}

/* PM2.5图形区域样式 */
.pm-view {
    background: linear-gradient(180deg, #9FBFFF 0%, #639AFF 100%);
    box-shadow: 0px 6rpx 12rpx rgba(66, 112, 196, 0.26);
}

/* 皮肤分析区域样式 */
.analysis {
    width: 694rpx;
    height: 1000rpx;
    background: #FFFFFF;
    opacity: 1;
    border-top-left-radius: 30rpx;
    border-top-right-radius: 30rpx;
    margin-top: 28rpx;
    margin-left: 28rpx;
    position: relative;
}

/* 皮肤分析标题区域样式 */
.analysis-title {
    height: 44rpx;
    display: flex;
    align-items: center;
}

/* 皮肤分析标题文字样式 */
.analysis-title-text {
    width: 192rpx;
    height: 44rpx;
    font-size: 32rpx;
    font-family: PingFang SC;
    font-weight: 500;
    line-height: 44rpx;
    color: #333333;
    opacity: 1;
    margin-top: 16rpx;
    margin-left: 16rpx;
}

/* 皮肤分析标题右边框样式 */
.analysis-title-border {
    width: 8rpx;
    height: 32rpx;
    background: #FF75A0;
    opacity: 1;
    border-radius: 4rpx;
    margin-top: 16rpx;
    margin-left: 4rpx;
}

/* 皮肤分析总分区域样式 */
.analysis-score {
    width: 128rpx;
    height: 98rpx;
    line-height: 16px;
    color: #FF75A0;
    opacity: 1;
    margin-top: 28rpx;
    margin-left: 516rpx;
    display: flex;
    position: absolute;
}

/* 皮肤分析总分分数样式 */
.analysis-score-num {
    height: 100%;
    font-size: 80rpx;
    font-weight: 600;
    font-family: DIN-Black;
}

/* 皮肤分析总分单位样式 */
.analysis-score-unit {
    height: 100%;
    font-size: 40rpx;
    font-weight: bold;
    font-family: PingFang SC;
    margin-top: 16rpx;
}

.outer {
    position: relative;
}

/* 皮肤分析皮肤缺陷气泡上样式 */
.analysis-bubble-up {
    background: #FF75A0;
    box-shadow: 0px 6rpx 12rpx rgba(222, 88, 130, 0.28);
    opacity: 1;
    border-radius: 16rpx;
    font-size: 28rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 32rpx;
    color: #FFFFFF;
    opacity: 0;
    margin-top: 186rpx;
    margin-left: 24rpx;
    position: absolute;
    overflow: hidden;
    top: 80px;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10rpx;
}

/* 皮肤分析皮肤缺陷气泡下样式 */
.analysis-bubble-down {
    background: #FF75A0;
    box-shadow: 0px 6rpx 12rpx rgba(222, 88, 130, 0.28);
    opacity: 1;
    border-radius: 16rpx;
    font-size: 28rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 32rpx;
    color: #FFFFFF;
    opacity: 0;
    margin-top: 382rpx;
    margin-left: 466rpx;
    position: absolute;
    overflow: hidden;
    top: 80px;
    z-index: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10rpx;
}

/* 皮肤分析用户照片区域样式 */
.analysis-face {
    width: 544rpx;
    height: 544rpx;
    /* background: #FECFCE; */
    border-radius: 50%;
    opacity: 1;
    margin-top: 60rpx;
    margin-left: 76rpx;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* 用户拍照按钮区域样式 */
.analysis-face-uploader {
    position: absolute;
    z-index: 2;
    opacity: 0.6;
}

/* 自定义用户拍照按钮icon样式 */
.analysis-face-uploader-icon {
    width: 150rpx;
    height: 150rpx;
}

/* 皮肤分析用户照片区域用户照片样式 */
.analysis-face-image {
    width: 544rpx;
    height: 544rpx;
    border-radius: 50%;
    position: absolute;
}

/* 面部分析结果动画效果 */
.wrapper {
    width: 544rpx;
    height: 544rpx;
    background:
        linear-gradient(#fff, #fff),
        linear-gradient(90deg, #ffffff33 1px, transparent 0, transparent 19px),
        linear-gradient(#ffffff33 1px, transparent 0, transparent 19px),
        linear-gradient(transparent, #fff);
    background-size: 100% 1.5%, 10% 100%, 100% 8%, 100% 100%;
    background-repeat: no-repeat, repeat, repeat, no-repeat;
    background-position: 0% 0%, 0 0, 0 0, 0 0;
    /* 初始位置 */
    clip-path: polygon(0% 0%, 100% 0%, 100% 1.5%, 0% 1.5%);
    /* 添加动画效果 */
    animation: move 2s infinite linear;
    z-index: 1;
    border-radius: 50%;
}

@keyframes move {
    to {
        background-position: 0 100%, 0 0, 0 0, 0 0;
        /* 终止位置 */
        clip-path: polygon(0% 0%, 100% 0%, 100% 100%, 0% 100%);
    }
}

/* 皮肤分析用户历史分析记录区域样式 */
.history {
    width: 100%;
    height: 320rpx;
    margin-top: 28rpx;
    white-space: nowrap;
}

/* 皮肤分析用户历史分析记录单个区域样式 */
.history-item {
    width: 216rpx;
    height: 302rpx;
    background: #FFFFFF;
    box-shadow: 0px 4rpx 12rpx rgba(180, 21, 54, 0.19);
    opacity: 1;
    border-radius: 24rpx;
    margin-top: 10rpx;
    margin-left: 30rpx;
    display: inline-block
}

/* 皮肤分析用户历史分析记录单个区域图片样式 */
.history-item-face {
    width: 176rpx;
    height: 176rpx;
    background: #FFFFFF;
    /* border: 2rpx solid #707070; */
    border-radius: 50%;
    opacity: 1;
    margin-top: 20rpx;
    margin-left: 20rpx;
}

/* 皮肤分析用户历史分析记录单个区域图片样式 */
.history-item-face image {
    width: 176rpx;
    height: 176rpx;
    border-radius: 50%;
    position: absolute;
}

/* 皮肤分析用户历史分析记录单个区域日期样式 */
.history-item-date {
    width: 122rpx;
    height: 32rpx;
    background: #FF75A0;
    opacity: 1;
    border-radius: 16rpx;
    font-size: 24rpx;
    font-family: DIN;
    font-weight: 500;
    line-height: 30rpx;
    color: #FFFFFF;
    opacity: 1;
    margin-top: 8rpx;
    margin-left: 20rpx;
    text-align: center;
    position: absolute;
}

/* 皮肤分析用户历史分析记录单个区域状态样式 */
.history-item-state {
    height: 46rpx;
    margin-top: 40rpx;
    position: absolute;
}

/* 皮肤分析用户历史分析记录单个区域状态标题样式 */
.history-item-state-title {
    font-size: 20rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 28rpx;
    color: #777777;
    opacity: 1;
    margin-top: 16rpx;
    margin-left: 24rpx;
}

/* 皮肤分析用户历史分析记录单个区域状态分数样式 */
.history-item-state-num {
    font-size: 32rpx;
    font-family: DIN;
    font-weight: 500;
    line-height: 38rpx;
    color: #FF75A0;
    opacity: 1;
    margin-top: 8rpx;
}

/* 皮肤分析用户历史分析记录单个区域状态单位样式 */
.history-item-state-unit {
    font-size: 10px;
    font-family: PingFang SC;
    font-weight: 500;
    line-height: 14px;
    color: #FF75A0;
    opacity: 1;
    margin-top: 36rpx;
}

/* 皮肤详细分析模块样式 */
.detailed-analysis {
    width: 694rpx;
    background: #FFFFFF;
    opacity: 1;
    border-bottom-left-radius: 30rpx;
    border-bottom-right-radius: 30rpx;
    margin-left: 28rpx;
}

/* 标签栏根节点样式，设置内边距 */
.custom-class {
    padding-left: 20rpx;
}

/* 标签栏样式，减小选中时底部条和标签之间的距离 */
.nav-class {
    height: 70rpx;
    width: 500rpx;
}

/* 皮肤详细分析标题区域样式 */
.detailed-analysis-title {
    height: 40rpx;
    display: flex;
    align-items: center;
}

/* 皮肤详细分析标题点样式 */
.detailed-analysis-title-dot {
    width: 14rpx;
    height: 14rpx;
    background: #FF75A0;
    border-radius: 50%;
    opacity: 1;
    margin-top: 40rpx;
    margin-left: 30rpx;
}

/* 皮肤详细分析标题文字样式 */
.detailed-analysis-title-text {
    width: 168rpx;
    height: 40rpx;
    font-size: 24rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 40rpx;
    color: #333333;
    opacity: 1;
    margin-top: 42rpx;
    margin-left: 8rpx;
}

/* 皮肤详细分析内容样式 */
.detailed-analysis-content {
    font-size: 24rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 34rpx;
    color: #777777;
    opacity: 1;
    margin: 30rpx 52rpx;
}

/* 未绑定状态整体区域样式 */
.empty {
    width: 694rpx;
    height: 800rpx;
    background: #FFFFFF;
    opacity: 1;
    border-radius: 30rpx;
    margin-top: 28rpx;
    margin-left: 28rpx;
}

/* 空状态icon区域样式 */
.empty-icon {
    width: 456rpx;
    height: 544rpx;
    margin-top: 96rpx;
    margin-left: 114rpx;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* 空状态icon样式 */
.empty-icon image {
    width: 456rpx;
    height: 422rpx;
}

/* 空状态主标题样式 */
.empty-icon-title {
    font-size: 32rpx;
    font-family: PingFang SC;
    font-weight: 500;
    line-height: 44rpx;
    color: #333333;
    opacity: 1;
    margin-top: 30rpx;
}

/* 空状态副标题样式 */
.empty-icon-subtitle {
    font-size: 28rpx;
    font-family: PingFang SC;
    font-weight: 400;
    line-height: 40rpx;
    color: #777777;
    opacity: 1;
    margin-top: 8rpx;
}

/* 提示图区域样式 */
.tips {
    width: 602rpx;
    height: 602rpx;
    margin-top: 188rpx;
    margin-left: 74rpx;
}

/* 提示图区域图片样式 */
.tips-img {
    width: 602rpx;
    height: 602rpx;
}

/* 提示图区域关闭按钮视图样式 */
.tips-back {
    width: 78rpx;
    height: 78rpx;
    margin-top: 42rpx;
    margin-left: 262rpx;
}

/* 提示图区域关闭按钮视图icon样式 */
.tips-back-img {
    width: 78rpx;
    height: 78rpx;
}